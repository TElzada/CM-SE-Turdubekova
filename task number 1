program epsilon
implicit none
real :: epsilon
integer :: n
n = 0
epsilon = 1.0

do
    epsilon = epsilon / 10
    n = n + 1
    if(1 + epsilon <= 1) then 
        exit
    end if
end do
    
    
    print *, "n:"
    print *, "eps", epsilon

end program epsilon
